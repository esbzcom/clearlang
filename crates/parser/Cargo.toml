[package]
name = "clg-parser"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
chumsky = "0.10.1"
clg-ast = { path = "../ast" }

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "parser"
harness = false
