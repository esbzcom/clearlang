import services::discount

function test_discount_uses_common_rate() -> Bool {
    discount::compute_discount(200) == 20
}

function test_discount_uses_promo_rate() -> Bool {
    discount::compute_discount(200) == 50
}
